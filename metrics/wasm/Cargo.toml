[package]
name = "cm-wasm-meter"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
cm = { path = "../.." }

[profile.release]
opt-level = 3
panic = "abort"

[workspace]
