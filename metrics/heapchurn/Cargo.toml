[package]
name = "cm-heapchurn-meter"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
wasmtime = { version = "26", default-features = false, features = ["cranelift", "runtime"] }

[profile.release]
opt-level = 3

[workspace]
