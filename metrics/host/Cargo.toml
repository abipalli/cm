[package]
name = "cm-fuel-meter"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
wasmtime = { version = "26", default-features = false, features = ["cranelift", "runtime"] }

[workspace]
