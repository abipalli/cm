# Entry 0062 — SCORE 573215 (+572)

| Field | Value |
|-------|-------|
| Date | 2026-06-17 |
| Author | @10d9e |
| Model | opus 4.8 |
| Git author | unknown \<unknown\> |
| Commit | `a377305` (a3773053072e6c7a1908aa07e7da77e936060c7a) |
| SCORE | 573215 |
| Δ vs previous record | +572 |
| vs zstd -22 | +17.13% |
| WORK | 9008114957 |
| Status | attempt |

## Approach

Output-neutral WORK reduction in the hot per-model predict/update loops (the 106 context models, run every bit). SCORE is byte-identical (573215, all six per-file sizes unchanged); only the executed-operator count drops, WORK 9.240G -> 9.008G (-2.5%) on scripts/measure-complexity.sh. Every change removes a per-element bounds check while keeping the exact same indices/values:
1. StateMap inner table changed from Vec<u32> to a fixed [u32; 2048] array (its length is the constant 256*8 for every model), with the index masked by the provable no-op & 2047 (mi = st<=255 | bitcount<8 << 8), so the access is bounds-check-free.
2. The byte accessor b() uses get_unchecked: buf.len() == bufmask+1 (a power of two), so p & bufmask is always valid.
3. The per-model table slot (predict row[ix], update tab[i][ix]) uses get_unchecked: ix is the in-range slot index chosen in predict (non-assoc h & tmask = len-1; assoc base+way < 2^tb).
4. The two stretch lookups use get_unchecked: stretch has 4096 entries and cp+2048 in [0,4095] (the counter is a contraction toward [0,4095]) and sm>>20 is a 12-bit value.
5. The outer per-model indices (tab[i], sm[i]) use get_unchecked: i < NCTX.
All indices are mathematically in-range; the adversarial round-trip suite passes and all six compressed sizes are unchanged, confirming byte-identical output.

## Iteration notes

Builds on the previous WORK PR (mixer loops, -43%). Measured WORK steps from 9.240G baseline: StateMap fixed-array -0.035G, b() unchecked -0.006G, tab unchecked -0.073G, stretch unchecked -0.038G, outer tab/sm unchecked -0.080G; total -0.232G (-2.5%). Verified byte-identical via all six per-file sizes (binary_mozilla 228283, repetitive_nci 13419, source_samba 175505, struct_xml 7476, text_dickens 90987, text_reymont 57545) and SCORE 573215; release round-trip (9 adversarial tests) passes. Each get_unchecked is guarded by a proven in-range invariant noted at the call site.

## Algorithm changes

```
 src/algorithm/model.rs | 30 ++++++++++++++++++++----------
 1 file changed, 20 insertions(+), 10 deletions(-)
```

## Eval snapshot

```
file                        orig      ours    ratio  vs zstd    vs xz  lossless
binary_mozilla.bin        393216    228283    1.722    +3.4%    +3.5%  OK
repetitive_nci.bin        393216     13419   29.303   +46.8%   +42.9%  OK
source_samba.bin          393216    175505    2.240   +10.9%    +9.4%  OK
struct_xml.bin            393216      7476   52.597   +49.4%   +45.8%  OK
text_dickens.bin          393216     90987    4.322   +27.6%   +26.7%  OK
text_reymont.bin          393216     57545    6.833   +38.0%   +36.7%  OK
--------------------------------------------------------------------------------
TOTAL                    2359296    573215    4.116
  vs zstd -22 total: 691699 bytes  ->  +17.13% (smaller, WIN)
  vs xz -9e   total: 682460 bytes  ->  +16.01% (smaller, WIN)

SCORE: 573215 (total compressed bytes; lower is better)
```
